# QuantPilot. Three dependencies, all of them load-bearing.
#
# Unlike OfflinePilotX, this project is not stdlib-only: screening 7,000
# symbols row-by-row in pure Python is roughly two orders of magnitude
# slower than the same filter as a pandas mask, and the whole design rests
# on the screen being unmetered and instant.
#
# The server and the UI have no dependencies at all — http.server and one
# self-contained HTML file.

requests>=2.31
pandas>=2.0
numpy>=1.24

# Real-time tick stream. yfinance ships the client and the generated
# protobuf types for Yahoo's websocket pricing feed, which is also how
# extended-hours prints arrive. Optional: app/stream.py probes for these
# and the terminal falls back to polling if they are absent.
yfinance>=1.3
websockets>=12.0
protobuf>=5.0
